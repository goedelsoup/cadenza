# Anthropic API key — copy to .env.local
# The web client uses dangerouslyAllowBrowser: true (your own key, local dev)
# For production, proxy through a server-side endpoint instead
VITE_ANTHROPIC_API_KEY=sk-ant-...
