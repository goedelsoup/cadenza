[package]
name        = "cadenza-theory"
description = "Music theory primitives — no_std compatible"
version.workspace   = true
edition.workspace   = true
license.workspace   = true

[features]
default = ["std"]
std     = []

[dependencies]
serde     = { workspace = true }
thiserror = { workspace = true }
