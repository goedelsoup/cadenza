{
  "name": "cadenza-web",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "root": "apps/cadenza-web",
  "sourceRoot": "apps/cadenza-web/src",
  "tags": ["lang:ts", "scope:app", "framework:svelte"],
  "implicitDependencies": ["cadenza-types", "cadenza-session", "cadenza-api", "cadenza-wasm"],
  "targets": {
    "dev": {
      "executor": "nx:run-commands",
      "dependsOn": ["cadenza-wasm:wasm:build:dev"],
      "options": { "command": "pnpm dev", "cwd": "apps/cadenza-web" }
    },
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": ["^build", "cadenza-wasm:wasm:build"],
      "outputs": ["{projectRoot}/dist"],
      "options": { "command": "pnpm build", "cwd": "apps/cadenza-web" }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": { "command": "pnpm test", "cwd": "apps/cadenza-web" }
    },
    "e2e": {
      "executor": "nx:run-commands",
      "options": { "command": "pnpm test:e2e", "cwd": "apps/cadenza-web" }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": { "command": "pnpm lint", "cwd": "apps/cadenza-web" }
    }
  }
}
